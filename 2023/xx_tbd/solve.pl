#!/usr/bin/perl -d:NYTProf

use strict;
use warnings;
use feature 'say';
use lib './lib';

use Data::Dumper;
use AOC::Base  qw(main);
use AOC::Math  qw(lcm);

sub part_one {
    my (@lines)    = @_;

    my $sum = 0;
    foreach my $line (@lines) {
        # todo
    }

    return $sum;
}

sub part_two {
    my (@lines)    = @_;

    my $sum = 0;
    foreach my $line (@lines) {
        # todo
    }

    return $sum;
}

main("xx_tbd", \&part_one, \&part_two);
